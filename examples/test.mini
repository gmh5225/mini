const foo: bool = false;

func add_two(x: int, y: int) -> int {
    return x + y;
}

// This is a comment
func main() -> int {
    x := -1;
    y := 2;
    z := 3;
    num := x + y * z;
    // num := -1 + 2 * 3;
    // Return value in variable `num` as status code
    return num;
}

/*
 ================================
 | output for linux_nasm_x86_64 |
 ================================
 section .data
     foo db 0

 section .text
     global _start
 
 _start:
     ; Evaluate -1 + 2 * 3
     mov rax, 2
     imul rax, 3
     add rax, -1

     ; Exit syscall
     mov rdi, rax
     mov rax, 60
     syscall
*/
