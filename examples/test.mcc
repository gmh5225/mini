const bool foo = false;

int add_two(int x, int y)
{
    return x + y;
}

// This is a comment
int main(int argc, char **argv)
{
    auto x = -1;
    auto y = 2;
    auto z = 3;
    auto num = x + y * z;
    return num;
}
